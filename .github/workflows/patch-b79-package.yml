name: Integrar gate B79

on:
  push:
    branches: [dev]
    paths:
      - ".github/workflows/patch-b79-package.yml"

permissions:
  contents: write

concurrency:
  group: patch-b79-package
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: dev
          fetch-depth: 0

      - name: Integrar verificador B79
        run: |
          node <<'NODE'
          const fs = require("node:fs");
          const path = "package.json";
          const packageJson = JSON.parse(fs.readFileSync(path, "utf8"));
          const scripts = packageJson.scripts ?? {};

          scripts["check:profile-contract-tests"] =
            "node scripts/check-profile-contract-tests.mjs";

          const marker = "npm run check:user-metadata-tests";
          const command = "npm run check:profile-contract-tests";
          if (typeof scripts.typecheck !== "string" || !scripts.typecheck.includes(marker)) {
            throw new Error("Marcador user-metadata não encontrado no typecheck.");
          }
          if (!scripts.typecheck.includes(command)) {
            scripts.typecheck = scripts.typecheck.replace(marker, `${marker} && ${command}`);
          }

          packageJson.scripts = scripts;
          fs.writeFileSync(path, `${JSON.stringify(packageJson, null, 2)}\n`, "utf8");
          NODE

      - name: Publicar integração e remover workflow temporário
        run: |
          rm .github/workflows/patch-b79-package.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add package.json .github/workflows/patch-b79-package.yml
          git commit -m "test(b79): integrar contratos de perfil ao typecheck"
          git push origin HEAD:dev
